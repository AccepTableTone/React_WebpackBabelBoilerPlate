import React, { Component } from 'react';
import { render } from 'react-dom';

import '../styles/site.css';

export default class App extends Component {
    render() {
        return (
            <div>
              Barebones Webpack Babel Sucka!!
            </div>
        );
    }
}
